python3 cli_ollama.py --project project_test
python3 cli_ollama.py --selector mcp_obt
python3 cli_ollama.py --clear_log


# Tento výpis obsahuje všechny lokální MCP nástroje; OBT nástroje začínají obt_.
python3 cli_mcp.py --list --out mcp_obt_tools.txt --no-db

# Privátní klíč se čte z kořenového .env (obt_key=***) a do API se neposílá.
# Každý požadavek má i parametr api_key=************* (lze změnit přes proměnnou OBT_API_KEY).

python3 cli_mcp.py --function obt_get_utxo --args '{}' --out obt_utxo.json --no-db

# Vypíše zeleně pouze číslo zůstatku a bez --no-db ho uloží do data/tasks.db pod selectorem mcp_obt.
python3 cli_mcp.py --function obt_get_balance --args '{}' --out obt_balance.txt
