{
  "version": 1,
  "table": "nostr_follows",
  "columns": [
    {"field": "uid", "type": "INTEGER", "constraints": ["PRIMARY KEY AUTOINCREMENT"]},
    {"field": "added_at", "type": "TEXT", "constraints": ["NOT NULL"]},
    {"field": "name", "type": "TEXT", "constraints": ["NOT NULL", "COLLATE NOCASE", "UNIQUE"]},
    {"field": "pubkey", "type": "TEXT", "constraints": ["NOT NULL"]}
  ],
  "indexes": [
    {"name": "idx_nostr_follows_added_at", "columns": ["added_at DESC", "uid DESC"]}
  ],
  "migrations": {}
}
