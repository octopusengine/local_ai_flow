# Install into the same Python environment used to run James / runner.py:
# python -m pip install -r requirements_laya.txt
laya>=0.3.4
# Explicit runtime dependencies (also normally installed transitively by laya).
# Minimum versions follow Laya's upstream pyproject.toml.
torch>=2.0.0
transformers>=4.45.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
numpy>=1.20.0
