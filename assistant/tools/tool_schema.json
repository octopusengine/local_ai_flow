{
  "version": 1,
  "tools": {
    "session_info": {
      "type": "function",
      "function": {
        "name": "session_info",
        "description": "Return current Code-session metadata: model, project, policy, schema profile, effective Ollama options, elapsed time, and tool-call count. It is read-only.",
        "parameters": {"type": "object", "properties": {}}
      }
    },
    "list_files": {
      "type": "function",
      "function": {
        "name": "list_files",
        "description": "List the files in a directory. Folders end with '/'.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Directory to list, e.g. '.'"}}, "required": ["path"]}
      }
    },
    "read_file": {
      "type": "function",
      "function": {
        "name": "read_file",
        "description": "Read a UTF-8 text file, optionally returning an inclusive range of line numbers.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Path of the file to read"}, "start_line": {"type": "integer", "minimum": 1, "description": "Optional first line number (1-based, inclusive)"}, "end_line": {"type": "integer", "minimum": 1, "description": "Optional last line number (1-based, inclusive)"}}, "required": ["path"]}
      }
    },
    "write_file": {
      "type": "function",
      "function": {
        "name": "write_file",
        "description": "Create or overwrite a text file with the given content.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Path of the file to write"}, "content": {"type": "string", "description": "Full contents of the file"}}, "required": ["path", "content"]}
      }
    },
    "run_command": {
      "type": "function",
      "function": {
        "name": "run_command",
        "description": "Run a shell command and return its output. Optionally pass stdin text to test an interactive program without manual typing. The user approves it first when run_confirm is enabled.",
        "parameters": {"type": "object", "properties": {"command": {"type": "string", "description": "The shell command to run"}, "stdin": {"type": "string", "description": "Optional text sent to the program's standard input, for example: '1\\n-3\\n2\\n'"}}, "required": ["command"]}
      }
    },
    "find_text": {
      "type": "function",
      "function": {
        "name": "find_text",
        "description": "Find literal text in project-local UTF-8 files. Results include file and line number; use path and glob to narrow the search.",
        "parameters": {"type": "object", "properties": {"query": {"type": "string", "description": "Text to find"}, "path": {"type": "string", "description": "Optional project-relative file or directory, default '.'"}, "glob": {"type": "string", "description": "Optional file pattern such as '*.py', default '*'"}, "case_sensitive": {"type": "boolean", "description": "Whether matching distinguishes upper and lower case; default false"}, "max_results": {"type": "integer", "minimum": 1, "maximum": 200, "description": "Maximum matching lines to return, default 50"}}, "required": ["query"]}
      }
    },
    "file_info": {
      "type": "function",
      "function": {
        "name": "file_info",
        "description": "Return safe metadata for one project-local file or directory: type, size, extension, and modification time.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Project-relative file or directory"}}, "required": ["path"]}
      }
    },
    "apply_patch": {
      "type": "function",
      "function": {
        "name": "apply_patch",
        "description": "Apply one verified patch to an existing project-local UTF-8 file. Prefer this for small edits instead of rewriting the full file.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Existing project-relative UTF-8 file to modify"}, "patch": {"type": "string", "description": "Either numbered unified diff hunks (@@ -old,+new @@), or *** Begin Patch / *** Update File: PATH / @@ / *** End Patch with unique source context"}}, "required": ["path", "patch"]}
      }
    },
    "python_runtime_info": {
      "type": "function",
      "function": {
        "name": "python_runtime_info",
        "description": "Detect the project .venv or venv, its Python interpreter, and whether pygame is installed. This does not create environments or install packages.",
        "parameters": {"type": "object", "properties": {}}
      }
    },
    "run_python": {
      "type": "function",
      "function": {
        "name": "run_python",
        "description": "Run one project-local Python source file with the project .venv or venv when available; otherwise use the current Python. It has a bounded timeout and returns a structured report for verification. It never installs packages.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Project-relative .py file to run"}, "args": {"type": "array", "items": {"type": "string"}, "description": "Optional command-line arguments, one array item per argument"}, "stdin": {"type": "string", "description": "Optional text sent to the program's standard input"}, "timeout_seconds": {"type": "integer", "minimum": 1, "maximum": 120, "description": "Optional run limit in seconds; default 30"}}, "required": ["path"]}
      }
    },
    "toolchain_info": {
      "type": "function",
      "function": {
        "name": "toolchain_info",
        "description": "Detect available C, C++, and Rust compiler commands on this computer. This is read-only; call it before compiling a non-Python program.",
        "parameters": {"type": "object", "properties": {}}
      }
    },
    "web_runtime_info": {
      "type": "function",
      "function": {
        "name": "web_runtime_info",
        "description": "Detect Node.js and locally available browser commands for project-local web development. This is read-only.",
        "parameters": {"type": "object", "properties": {}}
      }
    },
    "serve_project": {
      "type": "function",
      "function": {
        "name": "serve_project",
        "description": "Serve a project-local directory over a temporary localhost HTTP server for web testing. The server ends with the agent host process.",
        "parameters": {"type": "object", "properties": {"path": {"type": "string", "description": "Project-relative directory to serve, default '.'"}, "port": {"type": "integer", "minimum": 0, "maximum": 65535, "description": "Optional localhost port; use 0 or omit for an available port"}}}
      }
    },
    "browser_test": {
      "type": "function",
      "function": {
        "name": "browser_test",
        "description": "Read-only headless-browser check for a localhost URL returned by serve_project. It can verify that rendered DOM text is present; it never submits forms or visits external sites.",
        "parameters": {"type": "object", "properties": {"url": {"type": "string", "description": "Localhost URL returned by serve_project"}, "expected_text": {"type": "string", "description": "Optional rendered text expected in the DOM"}}, "required": ["url"]}
      }
    },
    "hardware_list_devices": {
      "type": "function",
      "function": {
        "name": "hardware_list_devices",
        "description": "List the configured external hardware and allowed named actions. Use this when the user asks what is available or a device/action is unclear; it never connects to hardware and never exposes BLE UUIDs, raw payloads, addresses, or secrets.",
        "parameters": {"type": "object", "properties": {}}
      }
    },
    "hardware_run_action": {
      "type": "function",
      "function": {
        "name": "hardware_run_action",
        "description": "Run exactly one configured named action. Use an action already established in this conversation, or call hardware_list_devices when it is unclear. Never invent device or action IDs. Only explicitly agent-enabled actions are accepted; raw BLE commands, UUIDs, payloads, and led-toggle are unavailable.",
        "parameters": {"type": "object", "properties": {"device_id": {"type": "string", "description": "Configured device ID returned by hardware_list_devices"}, "action_id": {"type": "string", "description": "Agent-enabled action ID returned by hardware_list_devices"}, "timeout_seconds": {"type": "number", "minimum": 1, "maximum": 60, "description": "Optional action timeout in seconds; default 15"}}, "required": ["device_id", "action_id"]}
      }
    },
    "system_datetime": {
      "type": "function",
      "function": {
        "name": "system_datetime",
        "description": "Return the current local system date and time with its UTC offset. It is read-only and does not modify project files.",
        "parameters": {"type": "object", "properties": {}}
      }
    },
    "network_ping": {
      "type": "function",
      "function": {
        "name": "network_ping",
        "description": "Ping the fixed diagnostic target 8.8.8.8 once and return reachability plus a concise result. It accepts no target parameter and does not modify project files.",
        "parameters": {"type": "object", "properties": {}}
      }
    }
  },
  "profiles": {
    "light": ["session_info", "list_files", "read_file", "write_file", "python_runtime_info", "run_python", "run_command"],
    "extended": ["session_info", "list_files", "read_file", "find_text", "file_info", "write_file", "apply_patch", "python_runtime_info", "run_python", "toolchain_info", "web_runtime_info", "serve_project", "browser_test", "run_command"],
    "hw_extended": ["session_info", "list_files", "read_file", "find_text", "file_info", "write_file", "apply_patch", "python_runtime_info", "run_python", "toolchain_info", "web_runtime_info", "serve_project", "browser_test", "run_command", "hardware_list_devices", "hardware_run_action"],
    "hardware": ["session_info", "list_files", "read_file", "find_text", "file_info", "python_runtime_info", "system_datetime", "network_ping", "hardware_list_devices", "hardware_run_action"]
  }
}
