# python .\runner.py flow_batch_body.txt
#
# Vnitřní flow - NEDEKLARUJE vlastní --project, spoléhá na to, že projekt
# už nastavila vnější flow_batch.txt (ta tenhle soubor i spouští jako
# vnořený běh runner.py, až POTÉ, co --batch zapsal batch_list.txt).
#
# Poznámka / známé omezení: tahle ukázka čeká TEXTOVÉ soubory (txt/md/html)
# ve zdrojovém adresáři. Binární typy (např. png) by potřebovaly jinou
# úlohu (popis obrázku) a větvení podle přípony souboru, což @if zatím
# neumí (jen file_exists/file_not_empty, ne "podle přípony") - viz TODO
# v cli_tool.md.

$batch_in = "src"
$batch_out = "dest"

# python3 cli_tool.py --batch

@for VAR in $batch
    python3 cli_tool.py --clr
    python3 cli_tool.py --add "zdrojový soubor" $batch_in/$VAR
    python3 cli_ollama.py --type task_base.json --context tools_context.txt --input "Stručně shrň obsah tohoto souboru ve 2-3 větách." --out batch_result.txt
    python3 cli_tool.py --copy batch_result.txt $batch_out/$VAR.txt
@endfor
